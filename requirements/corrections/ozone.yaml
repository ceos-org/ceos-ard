id: cozone
title: Ozone Corrections
description:
requirements:
  goal:
    description: |-
      Data is corrected for ozone.
  
      Relevant metadata must be provided under [@geometric-accuracy] and [@instrument].
  
      Metadata contains a single DOI landing page with references to:
      
      - a citable peer-reviewed algorithm
      - technical documentation regarding the implementation of that algorithm
    optional: true
dependencies:
  geometric-accuracy: metadata/geometric-accuracy-sr
  instrument: metadata/instrument-optical
glossary:
  - doi
references:
changes:
history:
  - SR 3.6
