id: valpix
title: Valid Pixels
description: 
requirements:
  threshold:
    description: |-
      Percentage of valid pixels in a specified area based on the applied flags from [@per-pixel].
dependencies:
sections:
  per-pixel: requirement-categories/per-pixel-metadata
glossary:
references:
changes:
history:
  - AR 1.17
