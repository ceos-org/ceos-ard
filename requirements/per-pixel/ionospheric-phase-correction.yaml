id: piopha
title: Ionospheric Phase Correction Image
description: 
requirements:
  goal:
    description: |-
      Phase correction value at each pixel, if applied.
  
      File format specifications/contents provided in metadata:
  
      - Sample Type (Angle)
      - Data Format (GeoTIFF, HDF5, NetCDF, …)
      - Data Type (Float, …)
      - Bits per Sample
      - Byte Order
    optional: true
dependencies:
glossary:
  - doi
  - url
references:
changes:
history:
  - SAR 2.16
